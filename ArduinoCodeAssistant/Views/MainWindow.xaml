<Window x:Class="ArduinoCodeAssistant.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:converters="clr-namespace:ArduinoCodeAssistant.Converters"
        xmlns:local="clr-namespace:ArduinoCodeAssistant.Views"
        mc:Ignorable="d"
        x:Name="AcaWindow"
        Title="아두이노 코드 어시스턴트" Height="600" Width="800">
    <Window.Resources>
        <converters:InverseBooleanConverter x:Key="InverseBooleanConverter"/>
    </Window.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="1*"/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" MinWidth="450"/>
            <ColumnDefinition Width="auto"/>
            <ColumnDefinition Width="*" MinWidth="220"/>
        </Grid.ColumnDefinitions>
        <DockPanel Grid.Row="0" Grid.Column="0" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="20 30 20 20">
            <StackPanel DockPanel.Dock="Right">
                <StackPanel Orientation="Horizontal" Margin="0 0 0 5">
                    <TextBlock Text="포트 번호:" VerticalAlignment="Center"/>
                    <TextBox IsReadOnly="True" Margin="10 0 0 0" Text="(읽기 전용) 텍스트" Height="30" VerticalContentAlignment="Center" HorizontalContentAlignment="Center" Width="200"/>
                </StackPanel>
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="기기 종류:" VerticalAlignment="Center"/>
                    <TextBox IsReadOnly="True" Margin="10 0 0 0" Text="(읽기 전용) 텍스트" Height="30" VerticalContentAlignment="Center" HorizontalContentAlignment="Center" Width="200"/>
                </StackPanel>
            </StackPanel>

            <Button Command="{Binding DetectArduinoButtonClickCommand}" DockPanel.Dock="Right" Margin="0 0 30 0" Padding="10" Content="아두이노 감지"/>
        </DockPanel>
        <GroupBox Grid.Row="1" Grid.Column="0" Header="명령 입력부" Margin="10 0 10 0">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition/>
                    <RowDefinition/>
                </Grid.RowDefinitions>
                <DockPanel Margin="10 10 10 15" Grid.Row="0">
                    <TextBlock Text="1. 현재 아두이노 보드 상태는? (ex. 3번 핀에 LED가 연결되어 있다.)" Margin="0 0 0 10" DockPanel.Dock="Top"/>
                    <TextBox ScrollViewer.VerticalScrollBarVisibility="Auto" Padding="5" Text="텍스트" TextWrapping="Wrap" AcceptsReturn="True"/>
                </DockPanel>
                <DockPanel Margin="10 0 10 10" Grid.Row="1">
                    <TextBlock DockPanel.Dock="Top" Text="2. 원하는 명령 입력" Margin="0 0 0 10"/>

                    <Button Padding="10" DockPanel.Dock="Top" Content="음성 인식 시작" Margin="0 0 0 10"/>
                    <TextBox ScrollViewer.VerticalScrollBarVisibility="Auto" DockPanel.Dock="Left" Padding="5" Text="텍스트" TextWrapping="Wrap" AcceptsReturn="True"/>

                </DockPanel>
            </Grid>
        </GroupBox>
        <DockPanel Margin="10" Grid.Row="2" Grid.Column="0">
            <CheckBox x:Name="AutoRequestCodeChk" DockPanel.Dock="Right" Margin="10" Content="자동 실행" VerticalAlignment="Center" HorizontalAlignment="Center"/>
            <Button Padding="10" Content="코드 생성 요청" DockPanel.Dock="Left" IsEnabled="{Binding ElementName=AutoRequestCodeChk, Path=IsChecked, Converter={StaticResource InverseBooleanConverter}}" />
        </DockPanel>
        <GridSplitter Grid.Row="0" Grid.Column="1" Grid.RowSpan="3" Width="5" VerticalAlignment="Stretch" HorizontalAlignment="Center"/>
        <Grid Margin="10" Grid.Row="0" Grid.Column="2" Grid.RowSpan="3">
            <Grid.RowDefinitions>
                <RowDefinition Height="*" MinHeight="135"/>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="*" MinHeight="90"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <GroupBox Margin="0 0 0 10" Padding="10" Grid.Row="0" Grid.ColumnSpan="2" Header="LLM 출력부">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition/>
                        <ColumnDefinition Width="10"/>
                        <ColumnDefinition/>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition/>
                        <RowDefinition Height="auto"/>
                    </Grid.RowDefinitions>
                    <DockPanel Grid.Column="0">
                        <TextBlock DockPanel.Dock="Top" Text="코드" Margin="0 0 0 5"/>
                        <TextBox ScrollViewer.HorizontalScrollBarVisibility="Auto" ScrollViewer.VerticalScrollBarVisibility="Auto" Padding="5" Text="텍스트" AcceptsReturn="True"/>
                    </DockPanel>
                    <DockPanel Grid.Column="2">
                        <TextBlock DockPanel.Dock="Top" Text="주석" Margin="0 0 0 5"/>
                        <TextBox ScrollViewer.HorizontalScrollBarVisibility="Auto" ScrollViewer.VerticalScrollBarVisibility="Auto" Padding="5" Grid.Column="0" Text="텍스트" AcceptsReturn="True"/>
                    </DockPanel>
                    <StackPanel Grid.Row="1" Grid.ColumnSpan="3" HorizontalAlignment="Center" VerticalAlignment="Center" Orientation="Horizontal" Margin="0 10 0 0">
                        <TextBlock Text="주석은  " VerticalAlignment="Center"/>
                        <ComboBox SelectedIndex="0">
                            <ComboBoxItem>일반적인 길이로</ComboBoxItem>
                            <ComboBoxItem>자세하게</ComboBoxItem>
                            <ComboBoxItem>간략하게</ComboBoxItem>
                        </ComboBox>
                        <TextBlock Text="  서술" VerticalAlignment="Center"/>
                    </StackPanel>
                </Grid>
            </GroupBox>
            <GridSplitter HorizontalAlignment="Stretch" VerticalAlignment="Center" Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="2" Height="5" ResizeDirection="Rows"/>
            <DockPanel Margin="0 10 0 0" Grid.Row="2" Grid.ColumnSpan="2">
                <DockPanel DockPanel.Dock="Top" Margin="0 0 0 10">
                    <CheckBox x:Name="AutoRequestCommunicationChk" DockPanel.Dock="Right" Margin="10" Content="자동 실행" VerticalAlignment="Center"/>
                    <Button Padding="10" Content="컴파일 및 통신" IsEnabled="{Binding ElementName=AutoRequestCommunicationChk, Path=IsChecked, Converter={StaticResource InverseBooleanConverter}}"/>
                </DockPanel>
                <TextBox ScrollViewer.HorizontalScrollBarVisibility="Auto" ScrollViewer.VerticalScrollBarVisibility="Auto" IsReadOnly="True" AcceptsReturn="True" Padding="5" Text="(읽기 전용) 로그 텍스트"/>
            </DockPanel>
        </Grid>
    </Grid>
</Window>
